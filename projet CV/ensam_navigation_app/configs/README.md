# Configurations

This directory is reserved for application, training, NLP, and deployment configuration files.

During the migration, runtime code still reads the current project paths directly. New configuration files should be introduced gradually so the Streamlit application remains compatible.
